name: Docker

# Triggers on the tag push itself, NOT only on release:published — the release
# is created by release.yml using the default GITHUB_TOKEN, and events created
# with that token never trigger other workflows (GitHub's recursion guard).
# release:published stays as a fallback for releases published manually in the UI.
on:
  push:
    tags: ["v*"]
  release:
    types: [published]
  # Manual backfill: run from the Actions tab with a v* tag selected as the ref.
  workflow_dispatch:

jobs:
  publish:
    name: Build & Push Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
